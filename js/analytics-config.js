/* ============================================================
   ANALYTICS CONFIG
   Paste the Web app URL from your Google Apps Script deployment
   (see analytics/apps-script.gs for setup). It's safe to publish:
   visitors can only add events, and reading them needs your
   access code, which is stored in Google, not here.
   Leave the placeholder in place to switch tracking off.
   ============================================================ */
window.ANALYTICS_CONFIG = {
  endpoint: 'PASTE-YOUR-APPS-SCRIPT-WEB-APP-URL-HERE'
};
